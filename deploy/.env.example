# Wave 量化分析系统 - 环境变量配置

# 数据目录（Docker 容器内路径）
DATA_DIR=/app/002484

# 策略参数
WINDOW_MINUTES=5
NUM_BINS=5
ENTRY_THRESHOLD=0.003
MIN_PATTERN_SAMPLES=10
WARMUP_DAYS=60
INITIAL_CAPITAL=100000

# 部署用户（默认 root）
DEPLOY_USER=root
